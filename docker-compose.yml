version: '3.8'

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # ИСПРАВЛЕННЫЙ путь - файл находится в директории nginx/
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /home/ivan/new_front/ssl/certificate.crt:/etc/nginx/ssl/certificate.crt:ro
      - /home/ivan/new_front/ssl/certificate.key:/etc/nginx/ssl/certificate.key:ro
    depends_on:
      - frontend
    networks:
      - common-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "8080"
    networks:
      - common-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  app-network:
    driver: bridge