# docker-compose.yml
version: '3.8'

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      # Монтируем файлы с правильными именами
      - /home/ivan/new_front/ssl/certificate.crt:/etc/nginx/ssl/certificate.crt:ro
      - /home/ivan/new_front/ssl/certificate.key:/etc/nginx/ssl/certificate.key:ro
    depends_on:
      - frontend
    networks:
      - app-network
    restart: unless-stopped
    # Добавим healthcheck для диагностики
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "8080"
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/"]
      interval: 30s
      timeout: 10s
      retries: 3

# Создаем собственную сеть (НЕ внешнюю)
networks:
  app-network:
    driver: bridge