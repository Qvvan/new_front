# Dragon VPN Frontend

Frontend приложение для Dragon VPN Mini App в Telegram.

## 🚀 Быстрый старт

### Локальная разработка

1. **Установите зависимости** (если требуется):
   ```bash
   npm install
   ```

2. **Запустите сервер разработки**:
   ```bash
   npm run dev
   ```

3. **Откройте в браузере**:
   ```
   http://localhost:3000
   ```

### Продакшн (Docker)

1. **Запустите через Docker Compose**:
   ```bash
   docker-compose up -d
   ```

2. **Приложение будет доступно**:
   - HTTP: `http://localhost:80`
   - HTTPS: `https://localhost:443`

## 📋 Доступные команды

- `npm run dev` - Запуск сервера разработки на порту 3000
- `npm start` - Запуск продакшн сервера на порту 8080
- `npm run tunnel` - Запуск ngrok туннеля для тестирования в Telegram

## ⚙️ Конфигурация

### Локальная разработка

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Настройте параметры:
- `PORT` - порт для локального сервера (по умолчанию 3000)
- `HOST` - хост для локального сервера (по умолчанию localhost)
- `NODE_ENV` - режим работы (development/production)

### Продакшн

Настройки продакшн находятся в:
- `docker-compose.yml` - конфигурация Docker контейнеров
- `nginx/nginx.conf` - конфигурация Nginx
- `Dockerfile` - образ для frontend сервера

## 📁 Структура проекта

```
├── assets/          # Статические ресурсы (изображения, анимации)
├── css/             # Стили приложения
├── js/              # JavaScript код
│   ├── api/         # API клиенты
│   ├── components/  # UI компоненты
│   ├── core/        # Основная логика приложения
│   ├── screens/     # Экраны приложения
│   ├── services/    # Сервисы
│   └── utils/       # Утилиты
├── nginx/           # Конфигурация Nginx
├── server.js        # Продакшн сервер
├── server.dev.js    # Сервер разработки
└── index.html       # Главная страница
```

## 🔧 Разработка

### Локальный запуск без Docker

Сервер разработки (`server.dev.js`) запускается на порту 3000 и включает:
- Логирование всех запросов
- Отключенное кеширование для быстрой разработки
- CORS заголовки для разработки
- Упрощенную обработку ошибок

### API

API запросы идут на `/api/*`. Для локальной разработки убедитесь, что:
- Бэкенд API доступен и настроен
- Или используйте проксирование (можно добавить в `server.dev.js`)

## 🌐 Запуск через ngrok (для тестирования в Telegram)

Для тестирования Telegram WebApp нужен публичный HTTPS URL. Ngrok создает туннель к вашему локальному серверу.

### Установка ngrok

1. **Скачайте ngrok**: https://ngrok.com/download
2. **Зарегистрируйтесь** (бесплатно) и получите authtoken
3. **Настройте ngrok**:
   ```bash
   ngrok config add-authtoken YOUR_AUTH_TOKEN
   ```

### Запуск с ngrok

**Вариант 1: Автоматический скрипт (рекомендуется)**

1. **Запустите локальный сервер** (в первом терминале):
   ```bash
   npm run dev
   ```

2. **Запустите ngrok туннель** (во втором терминале):
   ```bash
   npm run tunnel
   ```

**Вариант 2: Готовые скрипты**

- **Windows**: `scripts/start-ngrok.bat`
- **Linux/Mac**: `scripts/start-ngrok.sh`

**Вариант 3: Вручную**

```bash
ngrok http 3000
```

3. **Скопируйте HTTPS URL** из вывода ngrok (например: `https://abc123.ngrok-free.app`)

4. **Настройте Telegram Bot**:
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте `/setmenubutton`
   - Выберите вашего бота
   - Введите URL из ngrok: `https://abc123.ngrok-free.app`
   - Введите текст кнопки (например: "Открыть приложение")

5. **Откройте бота в Telegram** и нажмите на кнопку меню

### Важные замечания

- **Бесплатный ngrok**: URL меняется при каждом перезапуске
- **Платный ngrok**: Можно зафиксировать домен
- **API запросы**: Убедитесь, что бэкенд API также доступен через ngrok или использует публичный URL
- **Web интерфейс ngrok**: Доступен на `http://localhost:4040` для просмотра запросов и статистики

### Альтернативные способы

Если у вас есть свой домен с SSL, можно использовать:
- **Cloudflare Tunnel** (бесплатно, стабильный домен)
- **localtunnel** (альтернатива ngrok)
- **serveo.net** (SSH туннель)

## 📝 Примечания

- Для работы в Telegram WebApp требуется запуск через Telegram Mini App
- Локально можно тестировать UI, но функционал Telegram WebApp будет работать только в fallback режиме
- Все статические файлы должны быть явно указаны в `fileMap` или доступны через файловую систему
- При использовании ngrok убедитесь, что бэкенд API также доступен (через тот же туннель или отдельный)

